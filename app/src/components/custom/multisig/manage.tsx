import { Card, CardDescription } from "@/components/ui/card"
import { FC } from "react"

const Manage: FC = () => {
  return (
    <Card className="flex justify-center w-96 p-4">
      <CardDescription>
        Redirecting to your multisig...
      </CardDescription>
    </Card>
  )
}

export default Manage
